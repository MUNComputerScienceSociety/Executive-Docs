name: Update Docs
on: push
jobs:
  build:
    name: Update Docs
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v1
      - name: Compile Docs
        uses: ./.github/actions/compile-docs